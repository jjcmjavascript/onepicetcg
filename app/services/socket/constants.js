const DUEL_JOIN = 'duel:join';
const GAME_ROOM_JOIN = 'game:room_join';
const GAME_INIT_ROCK_PAPER_SCISSORS = 'game:initRockPaperScissors';

module.exports = {
  DUEL_JOIN,
  GAME_ROOM_JOIN,
  GAME_INIT_ROCK_PAPER_SCISSORS,
};
